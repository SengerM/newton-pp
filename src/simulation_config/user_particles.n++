vector<Particle> particles(N_PARTICLES);

for (j=0; j<N_PARTICLES; j++) {
	particles[j] = Particle();
	particles[j].Mass() = PARTICLES_MASS;
	particles[j].Position() = Vec3D(random_double(-3,3),random_double(-3,3),0);
}

